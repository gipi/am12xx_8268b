# Makefile for libiconv/test

TARGET_TYPE = app

ROOT_DIR  = ./../../../../..
IMAGE     = mlang_test
TARGET    = $(IMG_DIR)/$(IMAGE_APP)


INCS = $(SDK_DIR)/include 
LIBS = $(SDK_LIB_DIR)/libiconv.so

include $(ROOT_DIR)/scripts/common.mak

export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/mnt/card